#include "Relay.h"

Relay::Relay(int pin)
    : _pin(pin), _state(false) {}

void Relay::begin() {
    pinMode(_pin, OUTPUT);
    off();
}

void Relay::on() {
    _state = true;
    digitalWrite(_pin, HIGH);
}

void Relay::off() {
    _state = false;
    digitalWrite(_pin, LOW);
}