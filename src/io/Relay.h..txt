#ifndef RELAY_H
#define RELAY_H

#include <Arduino.h>

class Relay {
public:
    Relay(int pin);

    void begin();
    void on();
    void off();
    bool state() const { return _state; }

private:
    int _pin;
    bool _state;
};

#endif